import { ColorredDirective } from './colorred.directive';

describe('ColorredDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorredDirective();
    expect(directive).toBeTruthy();
  });
});
